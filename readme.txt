First all the images from database needs to be copied into working directory.
First train the database using dbtrain.m.
For discrete testing use discretetesting.m.
For bulk testing use bulktesting.m.
In case the files are less in database just change the number in dbtrain.

